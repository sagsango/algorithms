1. Finding the smallest cyclic shift  = suffixarray[0];
2. Finding a substring in a string    = O( |t| * log|t|  + |p| * log|t| )
                                      = suffix array formtation of text + binary search on suffix array for text
